import { createTheme } from '@mui/material'

const ActalentTheme = createTheme({
    palette: {
        mode: 'light',
        primary: {
          main: '#021a32',
        },
        secondary: {
          main: '#c1d82f',
        },
      },
})

export default ActalentTheme